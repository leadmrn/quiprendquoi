include ../partials/header.pug
h1 Qui prend quoi ?
h2= party.name
h3 Créé par #{party.author}

h2 Liste:
ul
  each item in party.items
    li= item.name
    form(method="post" action=`/party/${party._id}/items/${item._id}`)
      button(type="submit") Supprimer


form(method="post" action=`/party/${party._id}/items`)
  div
    label(for="name") Ce que je ramène :
    input(name="name" id="name" required minlength="3" placeholder="Vodka")
  div
    label(for="author" required) Nom :
    input(name="author" id="author" required minlength="3" placeholder="Lucas")
  button(type="submit") Ajouter


label(for="url") Partagez l'événement
input(id="url" value=`${url}` data-clipboard=`${url}` data-share-url=`${url}` data-share-title=`${party.name}` data-share-text=`${party.author} vous invite à rejoindre l'événement ${party.name}`)